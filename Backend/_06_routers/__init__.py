"""Imports every router below so main.py can do: from _06_routers import auth, users, ..."""


# from _06_routers.auth import auth
# from _06_routers.users import auth, users


"""
You're not required to add import lines here, 
because main.py imports each router file directly 
by name (from _06_routers import auth, users), not 
through this __init__.py. Remember — this file is 
only mandatory-with-real-imports for _02_models, 
because that's the one Alembic depends on. 
Everywhere else, it's optional convenience, 
and you've been choosing to leave it as just a
 docstring, which is fine.
"""