fastapi
uvicorn[standard]
sqlalchemy==2.0.51
alembic
psycopg2-binary
# Driver for SQLAlchemy's psycopg3 default on newer releases — keeps the
# `postgresql://` URL working regardless of which dialect SQLAlchemy picks.
psycopg[binary]
python-jose[cryptography]
passlib==1.7.4
bcrypt==4.0.1
pydantic
pydantic-settings
python-dotenv
python-multipart
pillow
email-validator
slowapi

# ML (v1 / v1.5 / v2 — add as you reach each phase)
# transformers
# scikit-learn
# sentence-transformers

# Dev
requests
