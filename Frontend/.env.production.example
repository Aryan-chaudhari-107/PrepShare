# PrepShare Frontend — Production Environment Configuration Example
# Copy this file to .env.production or set in your frontend hosting dashboard (e.g. Vercel, Netlify, Cloudflare Pages)

# Public HTTPS URL of the deployed FastAPI backend (NO trailing slash)
VITE_API_BASE_URL=https://YOUR-BACKEND-API-URL
